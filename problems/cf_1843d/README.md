### Thinking Process
#### Attempt 1:

I think the problem is basically asking about the number of leaves of some subtree rooted at x and some other subtree rooted at y, they added a touch of math. 
To find the number of leaves we will basically DFS starting from the subtree vertex downwards until we meet a leaf then we increment a counter.

We get the number of leaves of subtree x and subtree y, then multiply them, I think that's the answer.

Accepted.